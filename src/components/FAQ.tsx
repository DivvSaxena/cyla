"use client"; import { useState } from "react"; import { faqs } from "@/src/lib/site";
export function FAQ() { const [openIndex, setOpenIndex] = useState<number | null>(null); return <section className="section faq" id="faq" aria-labelledby="faq-title"><div className="container narrow"><p className="eyebrow">Questions, answered</p><h2 id="faq-title">FAQ</h2><div className="faq-list">{faqs.map(([question, answer], index) => { const open = openIndex === index; return <div className="faq-item" key={question}><h3><button aria-expanded={open} aria-controls={`faq-answer-${index}`} onClick={() => setOpenIndex(open ? null : index)}><span>{question}</span><span className="faq-symbol" aria-hidden="true">{open ? "−" : "+"}</span></button></h3><div id={`faq-answer-${index}`} className="faq-answer" hidden={!open}><p>{answer}</p></div></div>; })}</div></div></section>; }
